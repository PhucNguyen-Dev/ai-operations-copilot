'use client'
import { useEffect, useState } from 'react'
import ChatPanel from '@/components/chat/ChatPanel'
export default function AgentBubble({canUse}:{canUse:boolean}){const [open,setOpen]=useState(false),[size,setSize]=useState<'small'|'medium'|'full'>('small'),[sessionId,setSessionId]=useState<string|null>(null);useEffect(()=>{if(!open)return;const onKey=(e:KeyboardEvent)=>{if(e.key==='Escape')setOpen(false)};window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[open]);useEffect(()=>{if(canUse){const key='askx-session';setSessionId(localStorage.getItem(key));}},[canUse]);if(!canUse)return null;const store=(id:string)=>{setSessionId(id);localStorage.setItem('askx-session',id)};return <><button onClick={()=>setOpen(true)} aria-label="Open Ask X assistant" className="fixed bottom-5 right-5 z-50 grid h-14 w-14 place-items-center rounded-full bg-[var(--brand)] text-2xl text-white shadow-lg transition hover:scale-105">✦</button>{open&&<div ref={(el)=>{if(el)el.classList.toggle('theme-dark',(localStorage.getItem('askx-theme')??localStorage.getItem('theme'))!=='light')}} className={'askx-surface theme-dark fixed z-50 flex flex-col overflow-hidden rounded-2xl border border-[var(--border)] bg-[var(--card)] shadow-2xl '+(size==='full'?'inset-3':size==='medium'?'bottom-5 right-5 h-[min(72vh,680px)] w-[min(620px,calc(100vw-2rem))]':'bottom-5 right-5 h-[min(520px,calc(100vh-7rem))] w-[min(430px,calc(100vw-2rem))]')}><header className="flex items-center justify-between border-b border-[var(--border)] px-4 py-3"><div><p className="font-semibold text-[var(--ink)]">Ask X</p><p className="text-xs text-[var(--ink-3)]">Governed operations assistant</p></div><div className="flex items-center gap-1"><button onClick={()=>setSize(size==='small'?'medium':size==='medium'?'full':'small')} className="rounded-lg px-2 py-1 text-xs text-[var(--ink-2)] hover:bg-[var(--surface-2)]">{size==='full'?'↙':'↗'}</button><button onClick={()=>setOpen(false)} className="rounded-lg px-2 py-1 text-lg text-[var(--ink-2)]">×</button></div></header><ChatPanel sessionId={sessionId} onSessionIdChange={store}/></div>}</>}
